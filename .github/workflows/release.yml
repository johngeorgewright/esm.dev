name: Publish

on:
  push:
    branches: [master]

concurrency:
  group: release

jobs:
  publish-npm:
    name: NPM
    runs-on: ubuntu-latest
    outputs:
      new_release_published: ${{ steps.release.outputs.new_release_published }}
      version: ${{ steps.release.outputs.new_release_version }}
    steps:
      - name: Checkout project
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Use Bun
        uses: oven-sh/setup-bun@v2

      - name: Install
        run: bun install

      - name: Test
        run: bun run test

      - name: Semantic Release
        uses: cycjimmy/semantic-release-action@v4
        id: release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          extra_plugins: |
            @semantic-release/git

  push-image:
    needs: [publish-npm]
    if: needs.publish-npm.outputs.new_release_published == 'true'
    uses: johngeorgewright/workflows/.github/workflows/push-image.yml@main
    permissions:
      packages: write
    with:
      image-name: ghcr.io/johngeorgewright/esm.dev
      image-tag: ${{ needs.publish-npm.outputs.version }}
      registry: ghcr.io
    secrets:
      DOCKER_REPO_USERNAME: ${{ github.actor }}
      DOCKER_REPO_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
